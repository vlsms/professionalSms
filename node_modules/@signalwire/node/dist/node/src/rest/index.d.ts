import { StringStringMap } from '../../../common/src/util/interfaces';
import twilio = require('twilio');
declare const RestClient: (username: string, token: string, opts?: StringStringMap) => twilio.Twilio;
export default RestClient;
