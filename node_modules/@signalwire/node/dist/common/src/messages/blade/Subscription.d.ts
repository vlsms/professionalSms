import BaseMessage from '../BaseMessage';
import { IBladeSubscriptionRequest } from '../../util/interfaces';
declare class Subscription extends BaseMessage {
    method: string;
    constructor(params: IBladeSubscriptionRequest['params']);
}
export { Subscription };
