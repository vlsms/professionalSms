import BaseMessage from '../BaseMessage';
declare class Reauthenticate extends BaseMessage {
    method: string;
    constructor(project: string, jwt_token: string, sessionid: string);
}
export { Reauthenticate };
