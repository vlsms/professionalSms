export default class SendResult {
    protected result: {
        code: string;
        message: string;
        message_id: string;
    };
    successful: boolean;
    messageId: string;
    errors: string[];
    constructor(result: {
        code: string;
        message: string;
        message_id: string;
    });
}
