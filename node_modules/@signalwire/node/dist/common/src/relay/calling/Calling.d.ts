import { DeepArray, IDialCallParams, IMakeCallParams } from '../../util/interfaces';
import Relay from '../Relay';
import Call from './Call';
export default class Calling extends Relay {
    protected service: string;
    private _calls;
    notificationHandler(notification: any): void;
    newCall(...params: [IDialCallParams] | DeepArray<IMakeCallParams>): Call;
    dial(...params: [IDialCallParams] | DeepArray<IMakeCallParams>): Promise<import("./results").DialResult>;
    addCall(call: Call): void;
    removeCall(callToRemove: Call): void;
    getCallById(id: string): Call;
    getCallByTag(tag: string): Call;
    private _onState;
    private _onDial;
    private _onConnect;
    private _onReceive;
    private _onRecord;
    private _onPlay;
    private _onCollect;
    private _onFax;
    private _onDetect;
    private _onTap;
    private _onSendDigits;
}
