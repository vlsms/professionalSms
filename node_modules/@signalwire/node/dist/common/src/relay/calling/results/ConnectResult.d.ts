import { BaseResult } from './BaseResult';
import { Connect } from '../components/Connect';
import Call from '../Call';
export declare class ConnectResult extends BaseResult {
    component: Connect;
    constructor(component: Connect);
    get call(): Call;
}
