import { Controllable } from './Controllable';
import { IRelayCallingPlay } from '../../../util/interfaces';
import Call from '../Call';
export declare class Play extends Controllable {
    call: Call;
    play: IRelayCallingPlay[];
    volumeValue: number;
    eventType: string;
    method: string;
    controlId: string;
    constructor(call: Call, play: IRelayCallingPlay[], volumeValue?: number);
    get payload(): any;
    notificationHandler(params: any): void;
}
