import { Controllable } from './Controllable';
import { IRelayCallingDetect } from '../../../util/interfaces';
import Call from '../Call';
export declare class Detect extends Controllable {
    call: Call;
    private _detect;
    private _timeout;
    private _waitForBeep;
    eventType: string;
    method: string;
    controlId: string;
    type: string;
    result: string;
    private _events;
    private _waitingForReady;
    constructor(call: Call, _detect: IRelayCallingDetect, _timeout?: number, _waitForBeep?: boolean);
    get payload(): any;
    notificationHandler(params: any): void;
    private _complete;
}
