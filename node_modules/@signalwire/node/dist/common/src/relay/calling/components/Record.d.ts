import { Controllable } from './Controllable';
import Call from '../Call';
import { IRelayCallingRecord } from '../../../util/interfaces';
export declare class Record extends Controllable {
    call: Call;
    record: IRelayCallingRecord;
    eventType: string;
    method: string;
    controlId: string;
    url: string;
    duration: number;
    size: number;
    constructor(call: Call, record: IRelayCallingRecord);
    get payload(): any;
    notificationHandler(params: any): void;
}
