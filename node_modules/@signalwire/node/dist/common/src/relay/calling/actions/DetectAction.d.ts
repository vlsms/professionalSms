import { BaseAction } from './BaseAction';
import { Detect } from '../components';
import { DetectResult } from '../results/DetectResult';
export declare class DetectAction extends BaseAction {
    component: Detect;
    constructor(component: Detect);
    get result(): DetectResult;
    stop(): Promise<import("../results").StopResult>;
}
