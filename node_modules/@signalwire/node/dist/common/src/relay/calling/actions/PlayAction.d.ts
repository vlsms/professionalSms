import { BaseAction } from './BaseAction';
import { Play } from '../components/Play';
import { PlayResult, PlayPauseResult, PlayResumeResult, PlayVolumeResult } from '../results';
export declare class PlayAction extends BaseAction {
    component: Play;
    constructor(component: Play);
    get result(): PlayResult;
    stop(): Promise<import("../results").StopResult>;
    pause(): Promise<PlayPauseResult>;
    resume(): Promise<PlayResumeResult>;
    volume(value: number): Promise<PlayVolumeResult>;
}
