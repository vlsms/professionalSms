import { Controllable } from './Controllable';
import { IRelayCallingPlay, IRelayCallingCollect } from '../../../util/interfaces';
import Call from '../Call';
export declare class Prompt extends Controllable {
    call: Call;
    collect: IRelayCallingCollect;
    play: IRelayCallingPlay[];
    volumeValue: number;
    eventType: string;
    method: string;
    controlId: string;
    type: string;
    input: string;
    terminator: string;
    confidence: number;
    constructor(call: Call, collect: IRelayCallingCollect, play: IRelayCallingPlay[], volumeValue?: number);
    get payload(): any;
    notificationHandler(params: any): void;
}
