import Call from '../Call';
import { BaseFax } from './BaseFax';
export declare class FaxSend extends BaseFax {
    call: Call;
    private _document;
    private _identity;
    private _header;
    method: string;
    constructor(call: Call, _document: string, _identity?: string, _header?: string);
    get payload(): any;
}
