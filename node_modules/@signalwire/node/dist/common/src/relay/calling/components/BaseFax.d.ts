import { Controllable } from './Controllable';
export declare abstract class BaseFax extends Controllable {
    eventType: string;
    controlId: string;
    direction: string;
    identity: string;
    remoteIdentity: string;
    document: string;
    pages: number;
    notificationHandler(params: any): void;
}
