import { Controllable } from './Controllable';
import { IRelayCallingTapTap, IRelayCallingTapDevice } from '../../../util/interfaces';
import Call from '../Call';
export declare class Tap extends Controllable {
    call: Call;
    tap: IRelayCallingTapTap;
    device: IRelayCallingTapDevice;
    eventType: string;
    method: string;
    controlId: string;
    constructor(call: Call, tap: IRelayCallingTapTap, device: IRelayCallingTapDevice);
    get payload(): any;
    get sourceDevice(): IRelayCallingTapDevice;
    notificationHandler(params: any): void;
}
