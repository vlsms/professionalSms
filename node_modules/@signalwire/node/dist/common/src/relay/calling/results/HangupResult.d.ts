import { BaseResult } from './BaseResult';
import { Hangup } from '../components/Hangup';
export declare class HangupResult extends BaseResult {
    component: Hangup;
    constructor(component: Hangup);
    get reason(): string;
}
