import { BaseComponent } from './BaseComponent';
import { DeepArray, ICallDevice, IRelayCallingPlay } from '../../../util/interfaces';
import Call from '../Call';
export declare class Connect extends BaseComponent {
    call: Call;
    devices: DeepArray<ICallDevice>;
    ringback?: IRelayCallingPlay;
    eventType: string;
    method: string;
    controlId: string;
    constructor(call: Call, devices: DeepArray<ICallDevice>, ringback?: IRelayCallingPlay);
    get payload(): any;
    notificationHandler(params: any): void;
}
